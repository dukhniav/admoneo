from analyzer.utils import exceptions, repeatable_timer

# import logging
# import sched
# import time

# from ..utils import chat_helper, db_queries, exceptions, repeatable_timer
# # flake8: noqa: F401
# from .rpc import RPC, RPCException, RPCHandler


# (
#     MENU,
#     EDIT_COIN_LIST,
#     EDIT_USER_CONFIG,
#     DELETE_DB,
#     UPDATE_TG,
#     UPDATE_BTB,
#     PANIC_BUTTON,
#     CUSTOM_SCRIPT,
# ) = range(8)

# BOUGHT, BUYING, SOLD, SELLING = range(4)

# (CHOOSING, STATUS, TYPING_REPLY, TYPING_CHOICE) = range(4)

# logging.basicConfig(
#     format="%(asctime)s - %(name)s - %(levelname)s - %(message)s", level=logging.INFO
# )
# logger = logging.getLogger(__name__)

# scheduler = sched.scheduler(time.time, time.sleep)
